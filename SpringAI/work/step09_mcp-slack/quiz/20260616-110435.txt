다음 중 Java 25에서 도입된 Virtual Thread의 주요 특징으로 올바른 것은 무엇인가?

① Virtual Thread는 더욱 적은 메모리 오버헤드로 수천 개의 스레드를 생성할 수 있다.

② Virtual Thread는 CPU 자원을 독점적으로 사용하여 성능을 극대화한다.

③ Virtual Thread는 기존 스레드와 완전히 호환되지 않으며, 새로운 API를 필요로 한다.

④ Virtual Thread는 멀티코어 프로세서에서만 실행될 수 있다.