good weather~~
### React 배운 내용 요약 정리
1. React란?
2. 개발 환경 세팅(node.js 설치, 개발 도구(CRA, VITE))
3. JSX 문법(JavaScript, HTML을 함께 사용하는 것)
4. Component 개념
	- 화면을 
5. Hooks 종류
	- React는 클래스형 컴포넌트와 함수형 컴포넌트가 있다.
	- 클래스형 컴포넌트 시절에는 클래스 안에 컴포넌트의 상태, 라이프사이클에 관련된 함수들이 있어서, 이를 재정의해서 사용했다.
	  -> 하지만 클래스형 컴포넌트는 복잡하고 코드가 길다는 단점 존재
	  -> 그래서 함수형 컴포넌트를 사용하기 시작(함수 하나만 작성)
	- 그러다보니 컴포넌트의 상태, 라이프사이클을 관리할 수 없다.
	- 개발하다보면 컴포넌트의 상태, 라이프사이클 관리가 필요하다.
	- 그래서 React에서 Hooks라는 개념을 도입해서 컴포넌트의 상태, 라이프사이클 관리를 할 수 있게 되었다. (JavaScript에서 제공하는 함수들)
		1. 상태 관리
			1. useState, useReducer, useContext
		2. 라이프사이클 관리
			1. useEffect (mount, update, unmount)
		3. 성능 최적화
			1. useMemo, useCallback, React.memo
6. Router 개념
	- 페이지 전환(React SPA 이기 때문에 화면이 새로고침 되면 X)
	- 컴포넌트를 교체(page 단위)
	- `<a>` 태그를 사용하면 절대 안 됨!!!!
	- router-dom v7.x
7. axios 개념
	- Rest Service를 위한 라이브러리
	- 비동기 통신(Ajax) 기반으로 외부의 API와 연동(JSON으로 요청하고 응답)
	- **동기화 vs 비동기화 / JavaScript 싱글 스레드 개념 /  Web APIS 영역**
	- 가상의 서버(json-server 구축)
8. Redux
	1. 외부에서 중앙 집중적으로 상태를 관리해주는 외부 라이브러리
	2. 예를 들어 `store.js` 파일에 slice 단위로 상태 관리 변수를 선언하고, 함수들을 정의해서 export, configuration에 등록해 놓으면 필요한 곳에서 `useSelecter()`, `useDisPatch()` 을 선언해서 사용한다.

---
**전체를 적용한 프로젝트**
- todoList
- Fruits shop


---
# Linux
> 리눅스는 1991년에 리누스 토르발스 (Linus Torvalds)에 의해 개발된 오픈소스 운영체제이다.

- 리눅스는 유닉스를 모방하여 만들어진 운영체제로 맥 OS, 리눅스 등 많은 운영체제가 유닉스에 영향을 받아 만들어 졌다. 이로 인해 맥북과 리눅스는 유사한 운영체제 환경을 가지고 있다.
- 안드로이드 운영체제는 리눅스 커널을 기반으로 개발(2000년대 중반)되었다.
- 이후 많은 운영체제가 무료로 배포된 리눅스의 영향을 받아 만들어졌고, 리눅스 운영체제를 기본으로 다양한 프로그램을 추가한 수많은 배포판이 나오게 되었다.

### 리눅스 배포판
- **리눅스 배포판(Distribution, 줄여서 디스트로)은 리눅스 커널 + 필수 프로그램 + 패키지 관리자 + 설정 도구 등을 하나로 묶어서 누구나 쉽게 설치하고 사용할 수 있도록 만든 완성된 운영체제**이다.
- 즉, 리눅스를 사용하기 쉽게 **“포장한 버전”** 이라고 보면 된다.
- 리눅스는 원래 ‘커널(kernel)’만 있는 핵심 소프트웨어로 그 자체로는 쓸 수 없고, 우리가 쓰는 리눅스 시스템이 되려면 아래와 같이 요소가 필요하다.
- 위 요소들을 잘 조합하고 유지 보수해서 "사용자 친화적인 하나의 OS 패키지"로 만든 게 바로 리눅스 배포판이다.

**리눅스 배포판의 종류**
- Ubuntu는 초보자 친화적으로 포장
- Red Hat은 기업 환경에 맞게 포장
- Kali Linux는 보안 전문가용으로 포장
- Amazon Linux는 AWS에 최적화되게 포장

**패키지 관리자(Package Manager)란?**
- 패키지 관리자는 리눅스에서 소프트웨어(패키지)를 쉽게 설치, 업데이트, 삭제, 관리할 수 있게 해주는 도구이다.
- 마치 앱스토어처럼, 필요한 프로그램을 찾아서 설치하거나 새 버전이 나오면 자동으로 업데이트해줄 수도 있다.

### 리눅스 주요 개념
1. **커널**
	- 커널(kernel)은 컴퓨터 시스템의 핵심적인 부분으로, 하드웨어와 소프트웨어 간의 상호작용을 관리한다.( 메모리 관리, 입출력 관리, 프로세스 관리 등)
	- 즉, 리눅스 운영체제를 “커널”이라고 부르기도 한다.

2. **파일 시스템 구조**
	- 파일 시스템은 계층적 구조를 가지며, 가장 상위에는 루트 디렉토리 (/)
	- 주요 디렉토리로는 /bin, /etc, /home, /var 등이 있다.

|     |     |
| --- | --- |
|     |     |
|     |     |
|     |     |
|     |     |
|     |     |

3. **사용자와 그룹**
- 리눅스에서 사용자는 여러 그룹에 속하게 하여 그룹으로 관리한다.
- 권한 관리를 통해 특정 사용자나 그룹이 파일이나 디렉토리에 접근할 수 있는 권한을 지정한다.

3. 쉘(shell) 프로그램
- 사용자와 운영체제 간의 인터페이스 프로그램이다.
- 쉘은 사용자의 명령을 해석하여 운영체제 커널에 전달하는 것으로 대표적인 쉘 프로그램으로는 `bash(기본)`, `zsh`, `sh` 등
- `echo $0` 명령어로 현재 사용 중인 쉘 프로그램 확인 가능하다.

### 리눅스 명령어를 학습하기에 앞서
- Windows에서 Git을 설치하면 Git Bash가 함께 제공되며, 이를 통해 대부분의 리눅스 명령어를 사용할 수 있다.

### 명령어를 사용 할 수 있는 이유?
- Git Bash는 Git 과 함께 설치되는 터미널로, 리눅스와 유사한 Bash 셸 환경을 제공하여 `ls`, `cd`, `cp` 같은 기본적인 리눅스 명령어를 사용할 수 있다.
- Bash 셸은 본래 리눅스/유닉스 계열에서 사용하는 명령어 인터프리터로, Git Bash는 이를 윈도우에서 사용할 수 있도록 이식한 것이다. 따라서 윈도우에서도 리눅스처럼 명령어를 입력하고 작업할 수 있다.

### 한계점
- Git Bash에서 리눅스 명령어를 사용할 수 있지만, 완전한 리눅스 환경은 아니다.
	- systemctl, ps aux, top, apt-get 같은 시스템 관련 명령어는 사용할 수 없다.
	- 특정 프로그램(예: docker, kubectl)을 실행할 때 Windows 네이티브 프로그램과의 연동이 필요할 수 있다.

#### 완전한 리눅스 환경이 필요하다면?
- **WSL(Windows Subsystem for Linux) 사용**
	- Windows 10 이상에서는 WSL을 통해 실제 Ubuntu 등의 리눅스 배포판을 실행할 수 있다.
- **Docker의 Ubuntu 컨테이너 사용**
	- Docker에서 Ubuntu 컨테이너를 실행하면, 완전한 리눅스 환경에서 명령어를 실행할 수 있다.
- **Virtual Machine 사용**
	- VirtualBox, VMware 등으로 리눅스 가상 머신을 실행하는 방법도 있다.
- **AWS EC2사용**
	- AWS EC2 인스턴스를 생성하여 Linux 실행하는 방법도 있다.


**AWS 보안**
- 인바운드 규칙(밖 -> 안)
- 아웃바운드 규칙(안 -> 밖)

nano로 내용 수정하기
